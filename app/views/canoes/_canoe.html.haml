.thumbnail
  = link_to canoe_home_path(canoe) do
    = image_tag canoe.logo_url, class: 'img-responsive'
  .caption
    %h3= link_to canoe.title, canoe_home_path(canoe)
    %p= canoe.theme
    %p
      = link_to canoe_home_path(canoe), class: 'btn btn-primary' do
        논의
        = canoe.discussions.count
        개

      = link_to canoe_home_path(canoe), class: 'btn btn-default' do
        의견
        = canoe.opinions.count
        개

      = link_to canoe_home_path(canoe), class: 'btn btn-default' do
        선원
        = canoe.crews.count
        명
    %hr
    %p
      = user_byline canoe.user
      - if canoe.user == current_user
        %span.label.label-warning 만든이
