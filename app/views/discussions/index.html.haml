.bs-component
  .container
    .row
      .col-md-6
        .panel.panel-default
          .panel-heading
            %h4.text-muted
              %i.fa.fa-comments
              %strong= @unread_discussions.count
              개의 논의가 진행되었습니다.
          - if current_user.crewing_discussions.unread.any?
            %table.table.table-bordered
              %tbody
                = render @unread_discussions
          - else
            .panel-body
              %p.lead.text-muted.text-center 와아! 모두 읽으셨습니다!
      .col-md-6
        .panel.panel-default
          .panel-heading
            %h4.text-muted
              %i.fa.fa-comments
              승선 중인 카누들의 최근 논의
          %table.table.table-bordered
            %tbody
              = render @read_discussions
