.bs-component
  .container
    = render 'canoes_form'

    - if params[:q].present?
      %p.text-muted #{@canoes_count}건의 카누를 찾았습니다.

    - if @canoes_on_current_page.any?
      .row
        - @canoes_on_current_page.each do |canoe|
          .col-sm-4.col-xs-6
            = render canoe
      .row
        .col-xs-12.text-center
          = paginate @canoes_on_current_page, theme: 'twitter-bootstrap-3'

